/*
Project to practice using STL hash tables. Specifically, can practice:
1. Creating hash tables where custom hash functions are necessary (the
   4 methods to do this)

*/

#include "Point1.h"
#include "Point2.h"

#include <iostream>

int main(int argc, char* argv[])
{
    //Before adding to main, name from memory the 4 ways to create user
    //defined hash functions

    //Now, add function call to main that uses these 4 methods on new
    //class
    Test_Point1();
    Test_Point2();



    //keep console open
    std::cin.get();

    return 0;
}